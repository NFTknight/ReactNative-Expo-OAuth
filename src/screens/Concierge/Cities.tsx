import React from 'react';
import { View, Text } from 'react-native';

// ---------------------------------------------------------------- //

const Cities = ({ navigation }: { navigation: any }) => {

    return (
        <View style={{ flex: 1, backgroundColor: '#e4dfdb' }}>
            <Text>This is a Cities page</Text>
        </View>

    );
};

export default Cities;