import React from 'react';
import { View, Text } from 'react-native';

// ---------------------------------------------------------------- //

const Community = ({ navigation }: { navigation: any }) => {

    return (
        <View style={{ flex: 1 }}>
            <Text>This is a Community page</Text>
        </View>

    );
};

export default Community;