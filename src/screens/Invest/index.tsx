import React from 'react';
import { View, Text } from 'react-native';

// ---------------------------------------------------------------- //

const Invest = ({ navigation }: { navigation: any }) => {

    return (
        <View style={{ flex: 1 }}>
            <Text>This is a Invest page</Text>
        </View>

    );
};

export default Invest;